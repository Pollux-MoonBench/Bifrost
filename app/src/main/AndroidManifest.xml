<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <!-- Keep the LED pipeline warm while a PIPBOY override is active so the
         first action after idle isn't delayed by CPU power-down. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <!-- Plugin store: fetch the catalogue + download bundles. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.moonbench.bifrost.permission.CONTROL_LEDS"
        android:label="@string/perm_control_leds_label"
        android:description="@string/perm_control_leds_desc"
        android:protectionLevel="normal" />

    <uses-permission android:name="com.moonbench.bifrost.permission.CONTROL_LEDS" />


    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Bifrost">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-run guide launched from MainActivity.onCreate. Must be
             declared or R8 (minifyEnabled) renames the class and startActivity
             fails with ActivityNotFoundException, crashing app launch. -->
        <activity
            android:name=".StartupGuideActivity"
            android:exported="false" />

        <activity
            android:name=".plugins.PluginStoreActivity"
            android:exported="false"
            android:label="Plugin Store" />

        <service
            android:name=".services.LEDService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />

        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".external.ExternalApiReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.moonbench.bifrost.permission.CONTROL_LEDS">
            <intent-filter>
                <action android:name="com.moonbench.bifrost.api.ACTION_DISPLAY" />
                <action android:name="com.moonbench.bifrost.api.ACTION_CLEAR" />
                <action android:name="com.moonbench.bifrost.api.ACTION_INSTALL_PROFILE" />
                <action android:name="com.moonbench.bifrost.api.ACTION_UNINSTALL_PROFILE" />
                <action android:name="com.moonbench.bifrost.api.ACTION_PULSE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.PackageRemovedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_FULLY_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>